name: Build Distributions

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  release:
    types: [ published ]
  workflow_dispatch:

jobs:
  # ─── Windows: MSI + EXE ───────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        type: [ msi, exe ]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Build ${{ matrix.type }}
        run: ./gradlew.bat jpackage -PinstallerType=${{ matrix.type }}
        shell: cmd

      - name: Upload ${{ matrix.type }}
        uses: actions/upload-artifact@v4
        with:
          name: TarotBoard-windows-${{ matrix.type }}
          path: build/jpackage/TarotBoard-*.${{ matrix.type }}
          compression-level: 0
          if-no-files-found: error

  # ─── Linux: AppImage (directory) ─────────────────────────────────────
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install fakeroot
        run: sudo apt-get update -qq && sudo apt-get install -y -qq fakeroot

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Build AppImage
        run: ./gradlew jpackage -PinstallerType=app-image

      - name: Archive AppImage
        run: tar -czf TarotBoard-linux-appimage.tar.gz -C build/jpackage TarotBoard/

      - name: Upload AppImage archive
        uses: actions/upload-artifact@v4
        with:
          name: TarotBoard-linux-appimage
          path: TarotBoard-linux-appimage.tar.gz
          compression-level: 6
          if-no-files-found: error

  # ─── Android: APK ──────────────────────────────────────────────────
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Native Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libavcodec-dev libavformat-dev libavutil-dev \
            libgl-dev libglu1-mesa-dev libgtk-3-dev libpulse-dev \
            libswscale-dev libx11-dev libxi-dev libxtst-dev zlib1g-dev

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: true

      - name: Set up Gluon GraalVM
        uses: gluonhq/setup-gluon-graalvm@v1
        with:
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK platform & NDK
        run: |
          sdkmanager "platforms;android-34" "build-tools;34.0.0" "ndk;25.2.9519653"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.2.9519653" >> $GITHUB_ENV

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: android-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Build APK
        run: ./gradlew nativeBuild nativePackage -PandroidBuild=true --info --stacktrace
        env:
          GRAALVM_HOME: ${{ env.JAVA_HOME }}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TarotBoard-Android-APK
          path: build/gluonfx/arm64-android/gvm/*.apk
          if-no-files-found: error

      - name: Dump Linker Logs on Failure
        if: failure()
        run: cat build/gluonfx/arm64-android/gvm/log/native-image.log
